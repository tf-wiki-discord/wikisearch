name: Fly Deploy
on:
  push:
    branches:
      - main
env:
  FLY_API_TOKEN: "pwDh8VmlzZRH9qQRd0w9DA7LNKteuA8Z1pwE3ES-w_E"
jobs:
  deploy:
      name: Deploy app
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: superfly/flyctl-actions/setup-flyctl@master
        - run: flyctl deploy --remote-only
